training:
  test_size: 0.2
  random_state: 42

model:
  type: "RandomForest"
  n_estimators: 100
  max_depth: 10
  random_state: 42

preprocessing:
  normalize: true
  handle_missing: "mean"

mlflow:
  experiment_name: "iris_classification"

dataset:
  source: "sklearn"
  name: "iris"
